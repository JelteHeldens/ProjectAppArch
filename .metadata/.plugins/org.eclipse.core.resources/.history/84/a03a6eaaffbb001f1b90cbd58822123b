<!DOCTYPE html>
<html>

<head>
	<title>Handymen - Review Task</title>
	<div th:insert="/fragments/general.html :: head"></div>
</head>
<body>
<div th:insert="/fragments/general.html :: header"></div><br>

<div class="SinglePostit">
	<div class="postit">
		<div class="postitInner"> 
			<div class="postitTop">
				<div class="title" th:value="${task.title}"></div>
			</div>
			<div class="x" th:each="person : ${gebodenUserList}" >
				<form action="/gebodeTask" method="post">
					<!--<div class="description" th:text="${person.name}"></div>-->
					<input type="hidden" id="klusjesman" name="klusjesman" th:value="${person.email}">
					<input type="hidden" id="id" name="id" th:value="${huidigID}">
					<button type="submit" th:text="${person.name}+' [7.6/10]'" class="SubmitKlusje"></button>
				</form>
			</div>
		</div>
	</div>
</div><br><br>

<div class="addJobUitleg">
	<h1>Uitleg bij het beoordelen van een klusje:</h1>
	<a>Om het klusje volledig af te maken moet je de klusjesman beoordelen op diens werk.</a><br>
	<a>Beoordelen gebeurt door een getal op te geven tussen 0 en 10. Dit getal wordt meegerekend in de rating van de klusjesman.</a>
</div><br><br>

<div th:insert="/fragments/general.html :: footer"></div>
</body>
</html>