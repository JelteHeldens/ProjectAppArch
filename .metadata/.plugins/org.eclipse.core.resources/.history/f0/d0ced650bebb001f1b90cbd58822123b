<!DOCTYPE html>
<html>

<head>
	<div th:insert="/fragments/general.html :: head"></div>
	<title>Handymen</title>
	<script src="/js/script.js"></script>
</head>
<body onload="init()">
<div th:replace="/fragments/general.html :: header"></div><br>

	<div class="postitBoard">
		<a href="/newJob"class="ADDJOBBTN">VOEG JOB TOE<br> +</a>
		
		<div th:class="${'postit c-' + task.status.name()}" th:each="task : ${application.Opentasks}" >
		    <div class="postitInner">
				<div class="postitTop">
		        	<div class="title" th:text="${task.title}"></div>
					<div class="price" th:text="€+${task.price}"></div>
				</div><br>
		        <div class="description" th:text="${task.description}"></div>
				<div class="postitLower">
					<div class="owner" th:text="${task.owner}"></div>
					<div class="bids" th:text="${task.owner}"></div>
				</div>
		    </div>
		</div>
		
		<div th:class="${'postit c-' + task.status.name()}" th:each="task : ${application.Closedtasks}" >
		    <div class="postitInner">
				<div class="postitTop">
		        	<div class="title" th:text="${task.title}"></div>
					<div class="price" th:text="€+${task.price}"></div>
				</div><br>
		        <div class="description" th:text="${task.description}"></div>
		    </div>
		</div>
		
	</div>
	
<div th:insert="/fragments/general.html :: footer"></div>
</body>
</html>